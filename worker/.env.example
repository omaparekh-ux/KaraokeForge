# Model choices: htdemucs, mdx_q, etc.
DEMUCS_MODEL=htdemucs
# Use auto to select CUDA when available, otherwise CPU.
WHISPER_DEVICE=auto
WHISPER_MODEL=small
# float16 on CUDA, int8 on CPU.
WHISPER_COMPUTE=int8
VIDEO_PRESET=medium
