# Browser-visible worker URL. Required for large direct uploads.
NEXT_PUBLIC_KARAOKEFORGE_WORKER_URL=http://localhost:8000

# Optional server-side fallback/proxy URL.
KARAOKEFORGE_WORKER_URL=http://localhost:8000
